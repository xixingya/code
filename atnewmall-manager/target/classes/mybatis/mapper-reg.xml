<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.atnewmall.manager.dao.RegDao" >

    <select id="regUser" resultType="com.atguigu.atnewmall.bean.User">
        insert into t_member(loginacct,memberpswd) values(#{loginacct},#{userpswd})
    </select>
    
</mapper>